import { Config } from "./types";

export const config: Config = {
    app: {
        mailgen: {
            theme: 'default',
            product: {
                name: 'Neural Stream',
                link: 'https://www.neuralriver.ai',
            },
        },
    }
};
