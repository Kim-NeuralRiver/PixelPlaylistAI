export interface Config {
    app: {
        mailgen: {
            theme: string;
            product: {
                name: string;
                link: string;
            };
        };
    };
};